import { ButtonProps } from './button.interface';
/**
 * Primary UI component for user interaction
 */
export declare const Button: ({ primary, size, backgroundColor, label, type, className, children, ...props }: ButtonProps) => import("react/jsx-runtime").JSX.Element;
