export declare const textRecipe: import('@vanilla-extract/recipes').RuntimeFn<{
    variant: {
        body3: {
            fontSize: `var(--${string})`;
        };
        body2: {
            fontSize: `var(--${string})`;
        };
        body1: {
            fontSize: `var(--${string})`;
        };
        display: {
            fontSize: `var(--${string})`;
        };
        h5: {
            fontSize: `var(--${string})`;
        };
        h4: {
            fontSize: `var(--${string})`;
        };
        h3: {
            fontSize: `var(--${string})`;
        };
        h2: {
            fontSize: `var(--${string})`;
        };
        h1: {
            fontSize: `var(--${string})`;
        };
    };
}>;
