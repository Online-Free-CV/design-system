export declare const textRecipe: import('@vanilla-extract/recipes').RuntimeFn<{
    variant: {
        body3: {
            fontSize: `var(--${string})` | `var(--${string}, ${string})`;
        };
        body2: {
            fontSize: `var(--${string})` | `var(--${string}, ${string})`;
        };
        body1: {
            fontSize: `var(--${string})` | `var(--${string}, ${string})`;
        };
        display: {
            fontSize: `var(--${string})` | `var(--${string}, ${string})`;
        };
        h5: {
            fontSize: `var(--${string})` | `var(--${string}, ${string})`;
        };
        h4: {
            fontSize: `var(--${string})` | `var(--${string}, ${string})`;
        };
        h3: {
            fontSize: `var(--${string})` | `var(--${string}, ${string})`;
        };
        h2: {
            fontSize: `var(--${string})` | `var(--${string}, ${string})`;
        };
        h1: {
            fontSize: `var(--${string})` | `var(--${string}, ${string})`;
        };
    };
}>;
